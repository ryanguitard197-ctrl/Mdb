[package]
name = "mdb_os"
version = "0.1.0"
edition = "2021"

# Required: bare-metal x86_64, no OS underneath
[profile.dev]
panic = "abort"
opt-level = 1

[profile.release]
panic = "abort"
opt-level = 3
lto = true

[dependencies]
# Bootloader: produces a BIOS-bootable disk image via `cargo bootimage`
bootloader = "0.9.23"

# Hardware abstractions: page tables, registers, GDT, IDT
x86_64 = "0.14.10"

# Spinlock mutex (no OS scheduler yet, so no std Mutex)
spin = "0.9.8"

# UART serial output (QEMU -serial stdio)
uart_16550 = "0.2.19"

# 8259 PIC (hardware interrupt controller)
pic8259 = "0.10.4"

# PS/2 keyboard decoder
pc-keyboard = "0.7.0"

# no_std math: log2, floor, etc. (no libm in core)
libm = "0.2.8"

# Heap allocator for alloc:: (Vec, String, BTreeMap)
linked_list_allocator = "0.10.5"

# Static initialization without std
[dependencies.lazy_static]
version = "1.4"
features = ["spin_no_std"]

# ── bootimage config ─────────────────────────────────────────────────────────
# Run with:  cargo bootimage && qemu-system-x86_64 -drive format=raw,file=target/x86_64-mdb_os/debug/bootimage-mdb_os.bin
[package.metadata.bootimage]
test-args = [
    "-device", "isa-debug-exit,iobase=0xf4,iosize=0x04",
    "-serial", "stdio",
    "-display", "none"
]
test-success-exit-code = 33
